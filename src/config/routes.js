export const BASE_URL = "/";

const ROUTE_URLS = {
    HOME: BASE_URL,
    LOGIN: `${BASE_URL}login`,
    CONTACT: `${BASE_URL}contact-us`,
    ABOUT: `${BASE_URL}about`,
    PORT_LIST: `${BASE_URL}portfolio-list`,
    PORT_DETAILS: `${BASE_URL}portfolio-details`,
}

export default ROUTE_URLS;
